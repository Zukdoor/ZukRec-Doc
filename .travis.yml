language: node_js
node_js:
- '10'

branches:
  only:
  - master

cache:
  directories:
  - node_modules

script:
  - yarn build

before_deploy:
  - yarn global add now

deploy:
  provider: script
  script: bash scripts/deploy.sh
  on:
    branch: master
